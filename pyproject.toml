[project]
name = "training"
version = "0.1.0"
description = "Bus trip passenger-demand prediction, training, and inference"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "catboost>=1.2.10",
    "duckdb>=1.5.4",
    "fastapi>=0.115.0",
    "fastmcp>=3.0.0,<5",
    "matplotlib>=3.11.0",
    "pandas>=3.0.3",
    "scikit-learn>=1.9.0",
    "uvicorn[standard]>=0.34.0",
]
